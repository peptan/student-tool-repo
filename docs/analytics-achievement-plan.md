# 進数変換：通算の達成感と分析ページ

## 追加する記録

既存の「現在の10問セット」の到達状況とは別に、ブラウザ内で通算の学習記録を保持する。

- **変換方向別・初回正解率**：各10問セットを最初に解いた結果だけを累積する。復習結果は分母・分子に混ぜない。
- **難易度別・初回正解率**：4ビット=基礎、8ビット=標準、12ビット=発展として累積する。
- **通算で正解した問題数**：同じ変換・ビット幅・値の問題を一度以上正解したときだけ数える。復習で同じ問題を再正解しても増えない。

## 画面

- メニューには「学習の分析を見る」を追加する。
- `analytics.html` に、通算正解数、発展問題の正解数、変換方向別の初回正解率、難易度別の初回正解率を棒グラフで表示する。
- 初回 `1/10`、復習 `1/9` の例では、現在セットの到達状況 `2/10 (20%)` と、通算初回正解率を別項目にする。

## 保存の移行

旧形式は、復習用の誤答を保ち、現在セットの初回成績が復元できる場合だけ通算初回成績へ移す。不明な旧集計値を新しい指標へ混ぜない。

## 受入条件

1. 新しい10問を複数回解くと、変換方向別の初回正解率が累積する。
2. 復習の正解は通算初回正解率を変えない。
3. 12ビット問題を正解すると、発展問題の通算正解数に1問だけ加算される。
4. 分析画面はデータなし・データありの両方で読み取れる。
5. 問題文は4択を示す表現、採点結果はコンパクト表示、不正解は赤系にする。

## 検証ログ

- 自動確認：`npm test`（14件成功）、`npm run check`、`git diff --check` を実行。
- ローカル実ブラウザ：4択の問題文、選択・採点、全問解説、不正解の赤表示、コンパクトな答え比較、通算・変換別・難易度別の分析グラフを確認。コンソールエラーなし。